// Package renderer provides functions to render images
// from text in special fonts for 1-bit displays
package renderer

import "os"
import "errors"

// Render image from text using font
func RenderImage(text string, font string) (file *os.File, err error) {
    return nil, errors.New("Not implemented")
}
